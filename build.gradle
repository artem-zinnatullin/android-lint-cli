apply from: 'dependencies.gradle'

buildscript {
    apply from: 'dependencies.gradle'

    repositories {
        mavenCentral()
        google()
        jcenter()
    }

    dependencies {
        classpath gradlePlugins.shadow
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
        jcenter()
    }
}
